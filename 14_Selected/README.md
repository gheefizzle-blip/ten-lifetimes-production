# 14_Selected — LOCKED selects

Assets that passed Director → Gemini → EP review and were LOCKED. Per-sequence subfolders `Seq<NN>/`. Image + Dossier. Moved here from `12_Review_Queue/` on LOCK. (empty until the first hero locks)
